"use client";
import { GlobalWrapper } from "@/src/components";
import { Home } from "@/src/components";

export default function Page() {
    return (
        <main>
            <GlobalWrapper>
                <Home />
            </GlobalWrapper>
        </main>
    );
}
